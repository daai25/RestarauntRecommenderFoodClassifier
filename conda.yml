name: restaurant-recommend-classifier
channels:
    - anaconda
    - conda-forge
    - defaults
    - conda-forge
dependencies:
    - bokeh
    - jupyterlab
    - pandas
    - python
    - scikit-learn
    - scikit-plot
    - seaborn
    - geopy
    - pip
    - requests
    - pillow
    - pip:
          - python-dotenv
          - duckduckgo_search
          - torch
          - torchvision
          - torchaudio
          - --index-url=https://download.pytorch.org/whl/cu118 # this needs to be always at the end
