name: restaurant-recommend-classifier
channels:
    - anaconda
    - conda-forge
    - defaults
dependencies:
    - bokeh
    - jupyterlab
    - pandas
    - python
    - scikit-learn
    - scikit-plot
    - seaborn
    - numpy<2.0
    - opencv  # kompatible with numpy<2.0
    - pip
    - requests
    - pillow
    - tqdm
    - pip:
          - python-dotenv
          - torch
          - torchvision
          - torchaudio
          - --index-url=https://download.pytorch.org/whl/cu118 # this needs to be allways at the end
